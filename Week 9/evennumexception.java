public class evennumexception{
        public static void Checknumber(int number);
        throw Exception;{
            if(number%2==0);{
            Throw new.Exception("It is an even number"+number)}
        }
}
